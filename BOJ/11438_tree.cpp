#include<bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    int n,m;

    scanf("%d",&n);
    while(n--){
        int a,b;
        scanf("%d %d", &a, &b);
    }

    int m;
    scanf("%d",&m);
    while(m--) {
        int c,d;
        scanf("%d %d", &c, &d);
    }
    return 0;
}